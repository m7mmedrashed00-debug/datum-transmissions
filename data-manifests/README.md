Raw/calibrated data stays OUTSIDE git (controlled drive/NAS). Each run gets a manifest here:
run ID, UTC timestamp, operator, raw-file hash, fixture config, sensor IDs, calibration
versions, fluid state, firmware+analysis commits, decision-rule version, notes.
